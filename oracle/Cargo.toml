[package]
name = "oracle"
version = "7.1.0"
edition = "2021"

[lib]
crate-type = ["rlib"]

[features]
testutils = []

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"]  }
test-case = "*"